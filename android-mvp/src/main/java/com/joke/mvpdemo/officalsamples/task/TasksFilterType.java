package com.joke.mvpdemo.officalsamples.task;


public enum TasksFilterType {
    //Do not filter tasks
    ALL_TASKS,
    //Filters only the active ( not complete yet) tasks.
    ACTIVE_TASKS,
    //Filter only the completed tasks
    COMPLETED_TASKS
}
