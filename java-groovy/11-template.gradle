import groovy.text.*
import java.io.*

def template_1() {
    def file = new File('book.template')
    def binding = [
            'author':'ken',
            'title': 'Groovy',
            'publisher': "Elsevier",
            'isbn': '12345678'
    ]
    def engine = new SimpleTemplateEngine();
    def template = engine.createTemplate(file)
    def writable = template.make(binding)
    println writable //打印

    def destination = new FileWriter('book.xml')
    writable.writeTo(destination)
    destination.flush()
    destination.close() //输出到指定文件
}

//template_1()

def template_2() { //通过模板方式组合并并输出相应的内容
    def file = new File('library.template')
    def books = [
            '1' : ['Groovy','Ken','Elsevier'],
            '2' : ['Ojbect Oriented Design','John','Elsevier'],
            '3' : ['C Programming','Ken','Prentice Hall']
    ]
    def writable = new SimpleTemplateEngine().createTemplate(file).make(['books': books])
    println writable
    writable.writeTo(new FileWriter("librar.xml"))
}
template_2()
